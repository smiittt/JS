const name = "Smit Miyani";
for (const element of name) {
  // console.log(`Your name each char : ${element}`);
}

// maps

const okay = new Map();

okay.set("Smit", "92xx28xxx4");
okay.set("City", "Surat");
okay.set("Country", "India");
okay.set("Smit", "92xx28xxx4");

for (const [key, values] of okay) {
  // console.log(key +":-" + values);
}

const obj = {
    
};
